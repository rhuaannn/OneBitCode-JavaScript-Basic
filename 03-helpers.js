const tech = "Node.js";
const array = ["Node.js", "React", "TypeScript"];

const includes = tech.includes(".");
const includesArray = array.includes("Node.js");

console.log({ includesArray });

const startsWidth = tech.startsWith("ode");
const endWith = tech.endsWith(".js");

console.log({ endWith });
